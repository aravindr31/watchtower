---
import Auth from "../../components/Auth.astro";
import Layout from "../../layouts/Layout.astro";

// Astro.request.headers.set("Referer", `${Astro.url.}`);
// const token = Astro.cookies.get("access-token")?.value ?? null;
// console.log(token);
// if (token) Astro.request.headers.set("Authorization", `Bearer ${token}`);
---

<Layout title="watchtower">
  <Auth />
</Layout>
